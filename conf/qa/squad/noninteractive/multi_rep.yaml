parent_config: './conf/qa/squad/abstract_squad.yaml'
seed: 1337

repr_dim: 128
dropout: 0.2
epochs: 30

reader: 'multi_level_noninteractive_qa_reader'
name: 'multi_level_noninteractive_qa_reader'


prediction_levels: ['word', 'ctrl', 'segm', 'governor', 'assoc']

num_slots: 5

controller:
  encoder:
    - module: 'conv_glu'
      input: 'text'
      conv_width: 5
      num_layers: 2

